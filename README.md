# pubMLST Download Script

## Usage

```
usage: pubmlst_download.py [-h] [--scheme SCHEME] [--outdir OUTDIR]

optional arguments:
  -h, --help            show this help message and exit
  --scheme SCHEME, -s SCHEME
                        scheme to download
  --outdir OUTDIR, -o OUTDIR
                        output directory
```